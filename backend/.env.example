# GrazeScale AI Backend Environment Configuration (Render Ready)
# Option 1: Full Database Connection String (Recommended for Render PostgreSQL)
# DATABASE_URL=postgresql://user:password@hostname:5432/grazescale_db

# Option 2: Discrete Database Parameters
DB_HOST=localhost
DB_PORT=5432
DB_NAME=grazescale_db
DB_USER=postgres
DB_PASSWORD=your_secure_password

# API Configuration
PORT=8000
HOST=0.0.0.0
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,https://your-vercel-domain.vercel.app

# Model File Configuration
MODEL_PATH=muzzle_recognition_model.pkl
